class Player{
    constructor(x,y)
    {
        this.x=x
        this.y=y
        this.charecter=createSprite(this.x,this.y,1,1);
    }

    display()
    {
      // push();
      // imageMode(CENTER);
      // image(animation,this.charecter.position.x,this.charecter.position.y,100,100)
      // pop();
    }
}